const config = {
	startTime: Date.now(),
	user: {},
	room: {},
	tick: 0,
	skywayId: "",

	serverUrl: "https://" + window.location.host + "/",
	volumeAngle: [{
		angle: 180,
		len: 90
	}, {
		angle: 65,
		len: 180
	}]
}